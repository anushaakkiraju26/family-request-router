# Needed only to regenerate data/family_request_routing.csv via tools/generate_dataset.py.
# Defaults to Nebius AI Studio; point NEBIUS_BASE_URL + --model at a different
# OpenAI-compatible provider if you'd rather use one.
NEBIUS_API_KEY=
NEBIUS_BASE_URL=https://api.studio.nebius.com/v1/
